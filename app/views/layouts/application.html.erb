<!DOCTYPE html>
<html>
<head>
  <title>LinkBox</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="">
  <meta name="author" content="">
</head>

<body>
  <div class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container-fluid">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <%= link_to "Link Box", admin_signed_in? ? admin_root_path : root_path, :class => "brand" %>
        <div class="nav-collapse collapse">
          <% if admin_signed_in? -%>
          <p class="navbar-text pull-right">
            Logged in as <%= link_to current_admin.email, '#', :class => "navbar-link" %>
          </p>
          <% end -%>
          <ul class="nav">
            <li class="active"><%= link_to "Home", admin_signed_in? ? admin_root_path : root_path %></li>
            <% if admin_signed_in? -%>
            <li><%= link_to "Upload", upload_index_path %></li>
            <% end -%>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>
  </div>
  <div class="container-fluid">
    <% if alert or notice %>
    <div class="row-fluid">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
    </div>
    <% end %>
<%= yield %>
  </div>
</body>
</html>
